def Exercise2(matrix):
    result = []
    rows, columns = len(matrix), len(matrix[0])
    up = left = 0
    right = columns - 1
    down = rows - 1
    while len(result) < rows * columns:
        Travserve from left to 
    for i in matrix:
        
